{
  "scripts": {
    "ng": "ng",
    "start": "concurrently \"cd DynastyApp && ng serve\" \"cd dynasty-server && npm run start:dev\"",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "dependencies": {
    "@nestjs/axios": "^0.0.7",
    "axios": "^0.26.1",
    "concurrently": "^7.1.0"
  }
}